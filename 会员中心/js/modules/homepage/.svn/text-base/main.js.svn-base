/**
 * 
 */
define(["jquery","modules/homepage/homepage"],
    function ($,homepage) {
        return {
            init: function () {
                if ($(".uc-index-box").length > 0 ) {
                	homepage.init();
                }
            }
        };

    });
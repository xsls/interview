/**
 * 评价管理
 * Created by liuchuangui on 2014/4/19.
 */
define(["jquery", "modules/feedback/list"], function ($, list) {
        return {
            init: function () {
                if ($(".batch-evaluation-list").length > 0 ) {
                    list.init();
                }
            }
        };

});